<div class="network-router-3d">
   
   <!-- Heavy Extruded Edges -->
   <div class="router-edge top"></div>
   <div class="router-edge bottom"></div>
   <div class="router-edge left"></div>
   <div class="router-edge right"></div>

   <!-- Back Panel -->
   <div class="router-back">
      <div class="cable-ports">
         <div class="port"></div><div class="port"></div><div class="port"></div>
      </div>
   </div>

   <!-- Front Face -->
   <div class="router-front">
      <!-- Glow effect -->
      <div class="core-glow"></div>

      <!-- Surface Details -->
      <div class="router-brand">API-NET</div>
      
      <!-- Central Activity Matrix -->
      <div class="port-matrix">
         <div class="eth-port"><div class="led-tx"></div></div>
         <div class="eth-port"><div class="led-tx slow"></div></div>
         <div class="eth-port"><div class="led-tx fast"></div></div>
         <div class="eth-port"><div class="led-tx"></div></div>
         <div class="eth-port"><div class="led-tx slow"></div></div>
      </div>

      <!-- LED Indicators -->
      <div class="status-leds">
         <div class="s-led pwr"></div>
         <div class="s-led wan"></div>
         <div class="s-led lan"></div>
      </div>
   </div>

   <!-- Physical Antennas -->
   <div class="antenna ant-1"></div>
   <div class="antenna ant-2"></div>
</div>

<style>
/* ========================================
   CONCRETE 3D NETWORK ROUTER
   ======================================== */
.network-router-3d {
   width: 320px; height: 160px;
   position: relative;
   transform-style: preserve-3d;
}

/* Antennas projecting out */
.antenna {
   position: absolute; width: 15px; height: 120px;
   background: linear-gradient(to right, #1e293b, #475569, #1e293b);
   border-radius: 8px 8px 0 0;
   bottom: 100%; z-index: -1; transform-origin: bottom center;
   border: 2px solid #0f172a; border-bottom: none;
}
.ant-1 { left: 40px; transform: translateZ(-20px) rotateZ(-30deg) rotateX(-20deg); }
.ant-2 { right: 40px; transform: translateZ(-20px) rotateZ(30deg) rotateX(-20deg); }

.antenna::after {
   content: ''; position: absolute; top: 10px; left: 50%; transform: translateX(-50%);
   width: 6px; height: 6px; background: #ef4444; border-radius: 50%;
   box-shadow: 0 0 10px #ef4444; animation: blink-ant 1.5s infinite;
}

/* Base Extrusions (50px thick) */
.router-front {
   position: absolute; inset: 0; transform: translateZ(25px);
   background: linear-gradient(135deg, #0f172a, #020617);
   border-radius: 16px; border: 3px solid #334155; border-top: 4px solid #64748b;
   padding: 20px;
   box-shadow: inset 0 0 20px #000;
   transform-style: preserve-3d;
   display: flex; flex-direction: column; justify-content: space-between;
}
.router-back {
   position: absolute; inset: 0; transform: translateZ(-25px) rotateY(180deg);
   background: #1e293b; border-radius: 16px; border: 2px solid #334155;
   display: flex; justify-content: center; align-items: center;
}

.router-edge { position: absolute; background: #1e293b; box-shadow: inset 0 0 15px rgba(0,0,0,0.8); }
.router-edge.top { width: 288px; height: 50px; top: -25px; left: 16px; transform: rotateX(90deg); }
.router-edge.bottom { width: 288px; height: 50px; bottom: -25px; left: 16px; transform: rotateX(-90deg); border-bottom: 4px solid #000; }
.router-edge.left { width: 50px; height: 128px; left: -25px; top: 16px; transform: rotateY(-90deg); }
.router-edge.right { width: 50px; height: 128px; right: -25px; top: 16px; transform: rotateY(90deg); }

.core-glow {
   position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%) translateZ(-10px);
   width: 100px; height: 50px; background: rgba(0,245,255,0.2); filter: blur(20px);
}

.router-brand { color: #64748b; font-family: monospace; font-size: 1.2rem; font-weight: bold; letter-spacing: 3px; }

/* Status LEDs */
.status-leds { display: flex; gap: 10px; }
.s-led { width: 8px; height: 8px; border-radius: 50%; border: 1px solid #000; }
.pwr { background: #22c55e; box-shadow: 0 0 8px #22c55e; }
.wan { background: #00f5ff; box-shadow: 0 0 8px #00f5ff; animation: blink-ant 0.8s infinite; }
.lan { background: #00f5ff; box-shadow: 0 0 8px #00f5ff; animation: blink-ant 0.3s infinite alternate; }

/* Ethernet Ports */
.port-matrix {
   display: flex; gap: 8px;
   background: #000; padding: 10px; border-radius: 8px; border: 2px solid #1e293b;
   box-shadow: inset 0 5px 10px rgba(0,0,0,0.8);
}
.eth-port {
   flex-grow: 1; height: 25px; background: #1e293b; border-radius: 4px;
   position: relative; border-bottom: 2px solid #000;
}
.led-tx {
   position: absolute; top: 2px; right: 2px; width: 4px; height: 4px;
   background: #ef4444; border-radius: 50%; animation: tx-blink 0.1s infinite alternate;
}
.led-tx.slow { animation-duration: 0.5s; background: #eab308; }
.led-tx.fast { animation-duration: 0.05s; }

.cable-ports { display: flex; gap: 15px; }
.port { width: 25px; height: 25px; background: #000; border-radius: 4px; }

@keyframes blink-ant { 50% { opacity: 0.3; } }
@keyframes tx-blink { 100% { opacity: 0; } }
</style>
