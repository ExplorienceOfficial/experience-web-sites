<div class="globe-wrapper" style="--globe-color: #3b82f6; --globe-glow: rgba(59, 130, 246, 0.5); --globe-dark: rgba(59, 130, 246, 0.05);">
  <div class="mega-globe-3d">
    <div class="globe-scene">
      
      <!-- Hardware Projector Base -->
      <div class="projector-base">
         <div class="base-ring"></div>
         <div class="base-lens">
            <div class="lens-glare"></div>
         </div>
         <div class="base-beam"></div>
      </div>

      <!-- Holographic Earth -->
      <div class="hologram-earth">
         <!-- Longitude & Latitude Rings -->
         <div class="wireframe-sphere">
             <div class="latitude lat1"></div>
             <div class="latitude lat2"></div>
             <div class="latitude lat3"></div>
             
             <div class="longitude lon1"></div>
             <div class="longitude lon2"></div>
             <div class="longitude lon3"></div>
             <div class="longitude lon4"></div>
         </div>
         
         <!-- Data Nodes -->
         <div class="earth-nodes">
             <div class="node n1"><div class="pulse"></div></div>
             <div class="node n2"><div class="pulse"></div></div>
             <div class="node n3"><div class="pulse"></div></div>
             <div class="node n4"><div class="pulse"></div></div>
         </div>
      </div>

      <!-- Outer Data Rings -->
      <div class="orbit-rings">
          <div class="ring r1"><div class="orbit-dot"></div></div>
          <div class="ring r2"></div>
      </div>
      
    </div>
  </div>
</div>

<style>
/* ========================================
   MEGA CINEMATIC 3D GLOBE PROJECTOR
   ======================================== */
.globe-wrapper {
  width: 100%; display: flex; justify-content: center; align-items: center;
  padding: 60px 20px; background: transparent;
}

.mega-globe-3d {
  width: 100%; max-width: 500px; height: 400px;
  position: relative; perspective: 1200px; display: flex; align-items: center; justify-content: center;
  z-index: 50; cursor: pointer;
}

.globe-scene {
  width: 100%; height: 100%; position: relative; transform-style: preserve-3d;
  display: flex; align-items: center; justify-content: center;
  transform: rotateX(10deg) rotateY(0deg);
  transition: transform 0.8s cubic-bezier(0.25, 1, 0.5, 1);
}

/* HOVER EFFECT */
.mega-globe-3d:hover .globe-scene {
  transform: rotateX(0deg) rotateY(-15deg) scale3d(1.05, 1.05, 1.05);
}

/* Device Background Card */
.device-casing-mega {
  position: absolute; width: 300px; height: 380px;
  border: 1px solid var(--globe-glow);
  background: linear-gradient(180deg, var(--globe-dark), #020617);
  backdrop-filter: blur(5px);
  border-radius: 20px;
  transform: translateZ(-100px);
  z-index: 1; overflow: hidden;
  box-shadow: inset 0 0 40px rgba(59, 130, 246, 0.1);
  transition: all 0.5s ease;
}
.mega-globe-3d:hover .device-casing-mega {
  border-color: var(--globe-color);
  box-shadow: 0 0 30px #020617, inset 0 0 50px var(--globe-glow);
}
.casing-grid {
  position: absolute; inset: 0;
  background-image: linear-gradient(rgba(59,130,246,0.1) 1px, transparent 1px), linear-gradient(90deg, rgba(59,130,246,0.1) 1px, transparent 1px);
  background-size: 20px 20px; opacity: 0.5;
}
.casing-scanner {
  position: absolute; top: -10%; left: 0; width: 100%; height: 30px;
  background: linear-gradient(to bottom, transparent, var(--globe-glow)); opacity: 0; transition: opacity 0.3s;
}
.mega-globe-3d:hover .casing-scanner {
  opacity: 0.4; animation: global-scan 3s ease-in-out infinite;
}

/* Projector Base */
.projector-base {
  position: absolute; bottom: 20px; width: 150px; height: 40px;
  transform-style: preserve-3d; transform: rotateX(75deg); z-index: 5;
}
.base-ring {
  position: absolute; inset: 0; border: 3px solid #334155; border-radius: 50%;
  background: #0f172a; box-shadow: inset 0 10px 20px rgba(0,0,0,1), 0 10px 20px rgba(0,0,0,0.8);
}
.base-lens {
  position: absolute; top: 10px; left: 10px; right: 10px; bottom: 10px;
  border-radius: 50%; background: radial-gradient(circle, #fff, var(--globe-color), #020617);
  box-shadow: 0 0 30px var(--globe-color); animation: lens-breathe 2s infinite alternate; overflow: hidden;
}
.lens-glare {
  position: absolute; top: -50%; left: -50%; width: 200%; height: 200%;
  background: linear-gradient(45deg, transparent 40%, rgba(255,255,255,0.4) 45%, transparent 50%);
  animation: glare-spin 5s linear infinite;
}
.base-beam {
  position: absolute; top: -200px; left: 50%; width: 120px; height: 200px;
  transform: translateX(-50%) rotateX(-90deg); transform-origin: bottom center;
  background: linear-gradient(to top, var(--globe-glow), transparent);
  clip-path: polygon(20% 0, 80% 0, 100% 100%, 0 100%);
  pointer-events: none; opacity: 0.6;
}

/* Hologram Earth */
.hologram-earth {
  position: relative; width: 180px; height: 180px;
  transform-style: preserve-3d; z-index: 10;
  animation: spin-earth 20s linear infinite;
}
.wireframe-sphere {
  position: absolute; inset: 0; transform-style: preserve-3d;
}
.latitude, .longitude {
  position: absolute; inset: 0; border: 1px solid rgba(59, 130, 246, 0.3); border-radius: 50%; transform-style: preserve-3d;
}
.lat1 { transform: rotateX(30deg); } .lat2 { transform: rotateX(90deg); border: 2px solid rgba(59,130,246,0.8); } .lat3 { transform: rotateX(150deg); }
.lon1 { transform: rotateY(0deg); border: 2px solid rgba(59,130,246,0.8); } .lon2 { transform: rotateY(45deg); } .lon3 { transform: rotateY(90deg); border-width: 2px; } .lon4 { transform: rotateY(135deg); }

.earth-nodes { position: absolute; inset: 0; transform-style: preserve-3d; }
.node {
  position: absolute; width: 10px; height: 10px; background: #fff; border-radius: 50%;
  box-shadow: 0 0 15px 3px var(--globe-color); margin: -5px 0 0 -5px; transform-style: preserve-3d;
}
.pulse {
  position: absolute; inset: 0; border: 2px solid var(--globe-color); border-radius: 50%;
  animation: node-pulse 2s infinite ease-out;
}
.n1 { top: 20%; left: 30%; transform: translateZ(90px); }
.n2 { top: 60%; left: 70%; transform: translateZ(-70px); }
.n3 { top: 80%; left: 40%; transform: translateZ(60px); }
.n4 { top: 40%; left: 80%; transform: translateZ(30px); }

/* Outer Rings */
.orbit-rings {
  position: absolute; width: 260px; height: 260px; transform-style: preserve-3d;
}
.ring {
  position: absolute; inset: 0; border-radius: 50%; transform-style: preserve-3d;
}
.r1 { border: 2px dashed rgba(59,130,246,0.6); transform: rotateX(70deg) rotateY(10deg); animation: r-spin 15s linear infinite; }
.r2 { border: 1px solid rgba(255,255,255,0.1); transform: rotateX(60deg) rotateY(-20deg) scale(1.2); animation: r-spin 25s linear infinite reverse; }
.orbit-dot {
  position: absolute; top: 0; left: 50%; width: 8px; height: 8px; background: #fff; border-radius: 50%;
  box-shadow: 0 0 15px var(--globe-color); transform: translate(-50%, -50%);
}

/* KEYFRAMES */
@keyframes global-scan { 0% { top: -10%; } 50% { top: 110%; } 100% { top: -10%; } }
@keyframes lens-breathe { 0% { box-shadow: 0 0 20px var(--globe-color); } 100% { box-shadow: 0 0 50px var(--globe-color); } }
@keyframes glare-spin { 100% { transform: rotate(405deg); } }
@keyframes spin-earth { 100% { transform: rotateY(360deg); } }
@keyframes node-pulse { 0% { transform: scale(1); opacity: 1; } 100% { transform: scale(3); opacity: 0; } }
@keyframes r-spin { 100% { transform: rotateZ(360deg); } }
</style>
