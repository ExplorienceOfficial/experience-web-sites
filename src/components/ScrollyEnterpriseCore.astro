<div class="enterprise-server-3d">
   <!-- Extruded Edges -->
   <div class="server-edge top"></div>
   <div class="server-edge bottom"></div>
   <div class="server-edge left"></div>
   <div class="server-edge right"></div>
   
   <!-- Front Plate -->
   <div class="server-front">
      
      <!-- Top Vents -->
      <div class="vent-grid">
         <div class="vent-hole"></div><div class="vent-hole"></div><div class="vent-hole"></div><div class="vent-hole"></div>
      </div>

      <!-- Core Display Screen -->
      <div class="core-display">
         <div class="display-inner">
            <div class="circle-system">
               <div class="circle-line outline"></div>
               <div class="circle-line inner-spin"></div>
               <div class="status-core"></div>
            </div>
            <div class="lines-data">
               <div class="dataline l1"></div>
               <div class="dataline l2"></div>
               <div class="dataline l3"></div>
            </div>
         </div>
      </div>

      <!-- Drives Bays -->
      <div class="drive-bays">
         <div class="drive"><div class="drive-led active"></div></div>
         <div class="drive"><div class="drive-led"></div></div>
         <div class="drive"><div class="drive-led active fast"></div></div>
         <div class="drive"><div class="drive-led active"></div></div>
      </div>
   </div>

   <!-- Back Panel -->
   <div class="server-back">
      <div class="fan-unit">
         <div class="fan-blade"></div>
      </div>
   </div>
</div>

<style>
/* ========================================
   CONCRETE 3D ENTERPRISE SERVER TOWER
   ======================================== */
.enterprise-server-3d {
   width: 250px; height: 400px;
   position: relative;
   transform-style: preserve-3d;
}

/* Base Extrusions (40px thick) */
.server-front {
   position: absolute; inset: 0; transform: translateZ(20px);
   background: linear-gradient(135deg, #1e293b, #0f172a);
   border-radius: 12px; border: 2px solid #64748b;
   padding: 20px;
   box-shadow: inset 0 0 10px #000;
   transform-style: preserve-3d;
   display: flex; flex-direction: column; gap: 20px;
}
.server-back {
   position: absolute; inset: 0; transform: translateZ(-20px) rotateY(180deg);
   background: #0f172a; border-radius: 12px; border: 2px solid #475569;
   display: flex; align-items: center; justify-content: center;
}

.server-edge { position: absolute; background: #334155; box-shadow: inset 0 0 15px rgba(0,0,0,0.9); }
.server-edge.top { width: 226px; height: 40px; top: -20px; left: 12px; transform: rotateX(90deg); }
.server-edge.bottom { width: 226px; height: 40px; bottom: -20px; left: 12px; transform: rotateX(-90deg); }
.server-edge.left { width: 40px; height: 376px; left: -20px; top: 12px; transform: rotateY(-90deg); }
.server-edge.right { width: 40px; height: 376px; right: -20px; top: 12px; transform: rotateY(90deg); }

/* Vents */
.vent-grid { display: flex; gap: 5px; height: 40px; }
.vent-hole { flex-grow: 1; background: #020617; border-radius: 4px; box-shadow: inset 0 5px 10px #000; border-bottom: 2px solid #334155;}

/* Core Screen */
.core-display {
   width: 100%; height: 160px; background: #000; border-radius: 8px; border: 2px solid #334155;
   position: relative; overflow: hidden; display: flex; align-items: center; justify-content: center;
}
.display-inner {
   display: flex; gap: 15px; align-items: center; width: 100%; padding: 0 15px;
}
.circle-system { width: 70px; height: 70px; position: relative; display: flex; align-items: center; justify-content: center; }
.circle-line { position: absolute; border-radius: 50%; border: 2px solid transparent; }
.outline { width: 100%; height: 100%; border-color: rgba(16, 185, 129, 0.2); }
.inner-spin { width: 80%; height: 80%; border-top-color: #10b981; border-right-color: #10b981; animation: core-spin 2s linear infinite; }
.status-core { width: 40%; height: 40%; background: #10b981; border-radius: 50%; box-shadow: 0 0 15px #10b981; animation: pulse-core 2s infinite; }

.lines-data { flex-grow: 1; display: flex; flex-direction: column; gap: 8px; }
.dataline { height: 10px; background: rgba(16, 185, 129, 0.2); border-radius: 5px; overflow: hidden; position: relative; }
.dataline::after { content: ''; position: absolute; left: -100%; width: 50%; height: 100%; background: #10b981; animation: data-flow 2s infinite; }
.l2::after { animation-delay: 0.5s; width: 30%; }
.l3::after { animation-delay: 1s; width: 70%; }

/* Drives */
.drive-bays { flex-grow: 1; display: flex; flex-direction: column; gap: 8px; justify-content: flex-end;}
.drive { height: 25px; background: #1e293b; border-radius: 4px; border: 1px solid #000; border-bottom: 2px solid #475569; position: relative; }
.drive-led { width: 6px; height: 6px; border-radius: 50%; background: #111; position: absolute; right: 10px; top: 50%; transform: translateY(-50%); }
.drive-led.active { background: #3b82f6; box-shadow: 0 0 5px #3b82f6; animation: dr-blink 1s infinite alternate; }
.drive-led.active.fast { animation: dr-blink 0.2s infinite alternate; }

/* Back Fan */
.fan-unit { width: 120px; height: 120px; background: #000; border-radius: 50%; border: 5px solid #1e293b; box-shadow: inset 0 0 20px #000; position: relative; display: flex; justify-content: center; align-items: center; }
.fan-blade { width: 100px; height: 100px; background: conic-gradient(from 0deg, #334155 0deg 45deg, transparent 45deg 90deg, #334155 90deg 135deg, transparent 135deg 180deg, #334155 180deg 225deg, transparent 225deg 270deg, #334155 270deg 315deg, transparent 315deg 360deg); border-radius: 50%; animation: core-spin 0.5s linear infinite; }

@keyframes core-spin { 100% { transform: rotate(360deg); } }
@keyframes pulse-core { 50% { transform: scale(0.9); box-shadow: 0 0 5px #10b981; } }
@keyframes data-flow { 100% { left: 200%; } }
@keyframes dr-blink { 50% { opacity: 0.2; } }
</style>
