<div class="comms-wrapper" style="--highlight-color: #00f5ff; --holo-dark: rgba(0, 245, 255, 0.05); --holo-glow: rgba(0, 245, 255, 0.5);">
  <div class="mega-comms-3d">
    <div class="comms-scene">
      
      <div class="hologram-base">
          <div class="base-ring"></div>
          <div class="base-light"></div>
      </div>

      <div class="comms-core">
        <div class="core-inner"></div>
        <div class="core-pulse"></div>
        
        <div class="data-particles">
          <div class="particle p1"></div>
          <div class="particle p2"></div>
          <div class="particle p3"></div>
          <div class="particle p4"></div>
        </div>
      </div>

      <div class="data-rings">
        <div class="data-ring r1">
          <div class="ring-text">CONNECTING_</div>
        </div>
        <div class="data-ring r2"></div>
        <div class="data-ring r3"></div>
      </div>

      <div class="receiver-3d-mega">
          <div class="mega-shell">
              <div class="scanner-line"></div>
          </div>
      </div>
      
    </div>
  </div>
</div>

<style>
/* ========================================
   MEGA CINEMATIC 3D COMMS HUB (Gelişmiş)
   ======================================== */

.comms-wrapper {
  width: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 60px 20px;
  background: transparent; /* Kendi sitenin arka planına uyar */
}

.mega-comms-3d {
  width: 100%;
  max-width: 500px;
  height: 450px;
  position: relative;
  perspective: 1200px; /* Perspektif derinliğini artırdık */
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 50;
  cursor: pointer;
}

.comms-scene {
  width: 100%;
  height: 100%;
  position: relative;
  transform-style: preserve-3d;
  display: flex;
  align-items: center;
  justify-content: center;
  transform: rotateX(10deg) rotateY(0deg);
  transition: transform 0.8s cubic-bezier(0.25, 1, 0.5, 1);
}

/* HOVER EFEKTİ - Sahne Dönüşü */
.mega-comms-3d:hover .comms-scene {
  transform: rotateX(0deg) rotateY(-15deg) scale3d(1.05, 1.05, 1.05);
}

/* Hologram Base */
.hologram-base {
  position: absolute;
  bottom: 20px;
  width: 200px;
  height: 60px;
  transform-style: preserve-3d;
  transform: rotateX(75deg);
}
.base-ring {
  position: absolute; inset: 0;
  border: 2px solid var(--holo-glow);
  border-radius: 50%;
  box-shadow: 0 0 20px var(--holo-glow), inset 0 0 20px var(--holo-glow);
  animation: pulse-ring 3s infinite alternate;
}
.base-light {
  position: absolute;
  top: 50%; left: 50%;
  transform: translate(-50%, -50%);
  width: 50px; height: 50px;
  background: var(--highlight-color);
  border-radius: 50%;
  filter: blur(30px);
  opacity: 0.6;
}

/* Floating Central Orb */
.comms-core {
  position: relative;
  width: 100px; height: 100px;
  transform-style: preserve-3d;
  animation: float-orb 4s ease-in-out infinite alternate;
  z-index: 10;
  transition: all 0.5s ease;
}

.mega-comms-3d:hover .comms-core {
  width: 120px; height: 120px;
  filter: brightness(1.3);
}

.core-inner {
  position: absolute; inset: 0;
  background: radial-gradient(circle at 30% 30%, #fff, var(--highlight-color), #0055ff);
  border-radius: 50%;
  box-shadow: 0 0 60px var(--highlight-color), inset 0 0 30px rgba(255, 255, 255, 0.8);
  animation: glow-breathe 3s ease-in-out infinite alternate;
}

.core-pulse {
  position: absolute; inset: -20px;
  border: 2px solid var(--highlight-color);
  border-radius: 50%;
  opacity: 0;
  animation: orb-pulse 2s cubic-bezier(0.1, 0.8, 0.3, 1) infinite;
}

/* Orbiting Particles */
.data-particles {
  position: absolute; width: 100%; height: 100%;
  transform-style: preserve-3d;
}

.particle {
  position: absolute; top: 50%; left: 50%;
  width: 8px; height: 8px;
  background: #fff;
  border-radius: 50%;
  box-shadow: 0 0 15px 3px var(--highlight-color);
  margin: -4px 0 0 -4px;
}

.p1 { animation: orbit-p 6s linear infinite; }
.p2 { animation: orbit-p 9s linear infinite reverse; }
.p3 { animation: orbit-p 12s linear infinite; }
.p4 { animation: orbit-p 15s linear infinite reverse; }

/* Data Rings */
.data-rings {
  position: absolute;
  width: 320px; height: 320px;
  transform-style: preserve-3d;
  transition: transform 0.5s ease;
}

.data-ring {
  position: absolute; inset: 0;
  border-radius: 50%;
  transform-style: preserve-3d;
}

.r1 { 
  border: 2px dashed rgba(0, 245, 255, 0.4); 
  transform: rotateX(70deg) rotateY(15deg); 
  animation: rotate-data 15s linear infinite; 
}
.r2 { 
  border: 1px solid rgba(255, 255, 255, 0.15); 
  transform: rotateX(60deg) rotateY(-20deg) scale(1.3); 
  animation: rotate-data 25s linear infinite reverse; 
}
.r3 { 
  border: 3px double rgba(0, 245, 255, 0.2);
  transform: rotateX(80deg) rotateY(0deg) scale(0.7); 
  animation: rotate-data 20s linear infinite; 
}

.ring-text {
  position: absolute; top: -15px; left: 50%;
  transform: translateX(-50%) rotateX(-90deg); /* Yazıyı dik tutmak için */
  font-family: 'Courier New', Courier, monospace;
  font-size: 0.85rem; font-weight: bold;
  color: var(--highlight-color);
  letter-spacing: 4px; text-shadow: 0 0 10px var(--highlight-color);
  opacity: 0.5; transition: opacity 0.3s;
}

.mega-comms-3d:hover .ring-text {
  opacity: 1;
  animation: text-glitch 0.2s infinite alternate;
}

/* Receiver Silhouette (Dev Arka Plan Kartı) */
.receiver-3d-mega {
  position: absolute;
  width: 280px; height: 380px;
  border: 1px solid var(--holo-glow);
  background: linear-gradient(180deg, var(--holo-dark), transparent);
  backdrop-filter: blur(5px);
  border-radius: 30px;
  transform: translateZ(-80px); /* Geriye ittik */
  z-index: 1;
  overflow: hidden;
  box-shadow: inset 0 0 40px rgba(0, 245, 255, 0.05);
  transition: all 0.5s ease;
}

.mega-comms-3d:hover .receiver-3d-mega {
  border-color: var(--highlight-color);
  box-shadow: 0 0 30px var(--holo-dark), inset 0 0 40px var(--holo-glow);
}

/* Tarayıcı Çizgisi (Scanner Line) */
.scanner-line {
  position: absolute;
  top: 0; left: 0;
  width: 100%; height: 2px;
  background: var(--highlight-color);
  box-shadow: 0 0 15px 5px var(--highlight-color);
  opacity: 0;
  transition: opacity 0.3s;
}

.mega-comms-3d:hover .scanner-line {
  opacity: 0.8;
  animation: scan-down 2s ease-in-out infinite;
}

/* KEYFRAMES */
@keyframes float-orb {
  0% { transform: translateY(15px); }
  100% { transform: translateY(-15px); }
}

@keyframes glow-breathe {
  0% { box-shadow: 0 0 30px var(--holo-glow); }
  100% { box-shadow: 0 0 80px var(--highlight-color); }
}

@keyframes orb-pulse {
  0% { transform: scale(1); opacity: 0.8; }
  100% { transform: scale(1.6); opacity: 0; }
}

@keyframes pulse-ring {
  0% { transform: scale(0.95); opacity: 0.5; }
  100% { transform: scale(1.05); opacity: 1; }
}

@keyframes orbit-p {
  0% { transform: rotateY(0deg) translateX(140px) rotateY(0deg); }
  100% { transform: rotateY(360deg) translateX(140px) rotateY(-360deg); }
}

@keyframes rotate-data {
  0% { transform: rotateZ(0deg); }
  100% { transform: rotateZ(360deg); }
}

@keyframes scan-down {
  0% { top: -10%; }
  50% { top: 110%; }
  100% { top: -10%; }
}

@keyframes text-glitch {
  0% { opacity: 1; }
  100% { opacity: 0.7; }
}

@media (max-width: 768px) {
  .mega-comms-3d { transform: scale(0.7); }
}
</style>