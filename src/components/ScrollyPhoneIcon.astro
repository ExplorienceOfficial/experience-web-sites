---
// Premium 3D Communication Hub Mockup (Photorealistic Desk Hardware)
---
<div class="comms-hub-3d">
   
   <!-- Heavy Titanium Base -->
   <div class="hub-edge top"></div>
   <div class="hub-edge bottom"></div>
   <div class="hub-edge left"></div>
   <div class="hub-edge right"></div>

   <!-- Back Panel -->
   <div class="hub-back">
      <div class="cable-port"></div>
      <div class="cable-port ext"></div>
      <div class="hub-logo">
          <svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="#52525b" stroke-width="2"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path></svg>
      </div>
   </div>

   <!-- Front Control Deck -->
   <div class="hub-front">
      <!-- Slanted Glass Display Area -->
      <div class="glass-display-area">
          <div class="screen-glare"></div>
          
          <div class="call-interface">
              <div class="caller-id">
                 <div class="avatar-ring"><div class="avatar-img"></div></div>
                 <div class="name-text">INCOMING CALL...</div>
                 <div class="number-text">GLOBAL HQ</div>
              </div>
              <div class="audio-wave">
                  <div class="wave-bar w1"></div><div class="wave-bar w2"></div><div class="wave-bar w3"></div><div class="wave-bar w4"></div><div class="wave-bar w5"></div>
              </div>
          </div>
      </div>

      <!-- Physical Control Board -->
      <div class="control-board">
         <!-- Keypad -->
         <div class="keypad-matrix">
            <div class="key">1</div><div class="key">2</div><div class="key">3</div>
            <div class="key">4</div><div class="key">5</div><div class="key">6</div>
            <div class="key">7</div><div class="key">8</div><div class="key">9</div>
            <div class="key">*</div><div class="key">0</div><div class="key">#</div>
         </div>
         
         <!-- Action Buttons -->
         <div class="action-buttons">
            <div class="btn-action b-red"><div class="led-ring"></div></div>
            <div class="btn-speaker"><div class="spkr-grill"></div></div>
            <div class="btn-action b-green"><div class="led-ring active"></div></div>
         </div>
      </div>
   </div>

   <!-- Floating Holographic Receiver (Characteristic Hover Effect) -->
   <div class="floating-receiver">
      <div class="receiver-handle">
         <div class="rx-earpiece"><div class="rx-grill"></div></div>
         <div class="rx-grip"></div>
         <div class="rx-mouthpiece"><div class="rx-grill"></div></div>
      </div>
      <!-- Energy beam connecting receiver to base -->
      <div class="energy-tether"></div>
   </div>
</div>

<style>
/* ========================================
   PREMIUM 3D DESK PHONE (Communication Hub)
   ======================================== */
.comms-hub-3d {
   width: 320px; height: 380px;
   position: relative; transform-style: preserve-3d;
   cursor: pointer;
}

/* Base Extrusions (40px thick base) */
.hub-front {
   position: absolute; inset: 0; transform: translateZ(20px);
   background: linear-gradient(135deg, #18181b, #09090b);
   border-radius: 20px; border: 2px solid #3f3f46; border-top: 2px solid #71717a;
   padding: 15px;
   box-shadow: inset 0 0 30px #000, 0 20px 40px rgba(0,0,0,0.6);
   transform-style: preserve-3d;
   display: flex; flex-direction: column; gap: 15px;
}
.hub-back {
   position: absolute; inset: 0; transform: translateZ(-20px) rotateY(180deg);
   background: #09090b; border-radius: 20px; border: 2px solid #27272a;
   display: flex; gap: 10px; justify-content: space-between; padding: 20px; align-items: flex-end;
}
.cable-port { width: 30px; height: 20px; background: #000; border: 1px solid #3f3f46; border-radius: 4px; box-shadow: inset 0 0 10px #000; }
.hub-logo { flex-grow: 1; display: flex; justify-content: center; opacity: 0.5; }

/* Metallic Bevel Edges */
.hub-edge { position: absolute; background: #27272a; box-shadow: inset 0 0 15px rgba(0,0,0,0.9); }
.hub-edge.top { width: 284px; height: 40px; top: -20px; left: 16px; transform: rotateX(90deg); background: #52525b; }
.hub-edge.bottom { width: 284px; height: 40px; bottom: -20px; left: 16px; transform: rotateX(-90deg); }
.hub-edge.left { width: 40px; height: 344px; left: -20px; top: 16px; transform: rotateY(-90deg); }
.hub-edge.right { width: 40px; height: 344px; right: -20px; top: 16px; transform: rotateY(90deg); }


/* Slanted Glass Display */
.glass-display-area {
   width: 100%; height: 160px; background: #000; border-radius: 12px;
   border: 1px solid #27272a; border-bottom: 3px solid #3f3f46;
   position: relative; overflow: hidden;
   /* Tilt it back slightly */
   transform: rotateX(10deg) translateZ(10px); transform-style: preserve-3d;
   box-shadow: 0 10px 20px rgba(0,0,0,0.5), inset 0 0 20px rgba(0,0,0,0.8);
}
.screen-glare {
   position: absolute; top: -50%; left: -50%; width: 200%; height: 200%;
   background: linear-gradient(135deg, rgba(255,255,255,0.08) 0%, transparent 40%);
   pointer-events: none; z-index: 10;
}

.call-interface {
   width: 100%; height: 100%; display: flex; flex-direction: column;
   align-items: center; justify-content: center; gap: 15px;
   background: radial-gradient(circle at center, #0f172a, #000);
}
.caller-id { display: flex; flex-direction: column; align-items: center; gap: 5px; }
.avatar-ring { 
   width: 50px; height: 50px; border-radius: 50%; border: 2px dashed #3b82f6; 
   display: flex; align-items: center; justify-content: center;
   animation: rotate-ring 6s linear infinite;
}
.avatar-img { width: 40px; height: 40px; background: #3b82f6; border-radius: 50%; box-shadow: 0 0 10px #3b82f6; }
.name-text { color: #fff; font-family: sans-serif; font-size: 0.9rem; font-weight: bold; letter-spacing: 1px; }
.number-text { color: #3b82f6; font-family: monospace; font-size: 1.1rem; }

.audio-wave { display: flex; gap: 3px; height: 20px; align-items: center; opacity: 0; transition: opacity 0.5s; }
.wave-bar { width: 4px; background: #10b981; border-radius: 2px; height: 20%; box-shadow: 0 0 8px #10b981; }

/* HOVER EFFECT: Answer Call */
.comms-hub-3d:hover .avatar-ring { border: 2px solid #10b981; animation: none; }
.comms-hub-3d:hover .avatar-img { background: #10b981; box-shadow: 0 0 15px #10b981; transform: scale(1.1); transition: all 0.3s; }
.comms-hub-3d:hover .name-text { color: #10b981; }
.comms-hub-3d:hover .number-text { color: #fff; }
.comms-hub-3d:hover .audio-wave { opacity: 1; }
.comms-hub-3d:hover .w1 { animation: wave-anim 0.8s infinite alternate; }
.comms-hub-3d:hover .w2 { animation: wave-anim 1.2s infinite alternate 0.2s; }
.comms-hub-3d:hover .w3 { animation: wave-anim 0.6s infinite alternate 0.4s; }
.comms-hub-3d:hover .w4 { animation: wave-anim 1s infinite alternate 0.1s; }
.comms-hub-3d:hover .w5 { animation: wave-anim 0.9s infinite alternate 0.3s; }

/* Physical Controls Area */
.control-board {
   flex-grow: 1; display: flex; flex-direction: column; gap: 15px; padding: 10px 5px;
   background: #18181b; border-radius: 12px; border: 1px solid #27272a;
   box-shadow: inset 0 0 20px rgba(0,0,0,1);
   transform-style: preserve-3d;
}

.keypad-matrix {
   display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; justify-items: center;
}
.key {
   width: 50px; height: 24px; background: linear-gradient(to bottom, #3f3f46, #27272a);
   border-radius: 4px; border: 1px solid #18181b; border-bottom: 2px solid #000;
   display: flex; align-items: center; justify-content: center;
   color: #a1a1aa; font-family: monospace; font-size: 1.1rem; font-weight: bold;
   box-shadow: 0 4px 6px rgba(0,0,0,0.5), inset 0 1px 1px rgba(255,255,255,0.1);
   transition: all 0.1s;
}

.action-buttons {
   display: flex; justify-content: space-between; align-items: center; padding: 0 15px;
}
.btn-action {
   width: 45px; height: 30px; border-radius: 15px; background: #27272a;
   border: 1px solid #18181b; border-bottom: 3px solid #000;
   position: relative; display: flex; justify-content: center; align-items: center;
   box-shadow: 0 5px 10px rgba(0,0,0,0.5);
}
.led-ring { width: 35px; height: 20px; border-radius: 10px; border: 2px solid #52525b; transition: all 0.3s; }
.btn-speaker {
   width: 80px; height: 40px; border-radius: 20px; background: #18181b;
   border: 2px solid #3f3f46; border-top: 1px solid #52525b;
   display: flex; justify-content: center; align-items: center;
}
.spkr-grill {
   width: 60px; height: 20px; border-radius: 10px;
   background-image: radial-gradient(#000 30%, transparent 35%); background-size: 4px 4px;
}

.b-red .led-ring.active { border-color: #ef4444; box-shadow: inset 0 0 10px #ef4444, 0 0 10px #ef4444; }
.b-green .led-ring.active { border-color: #10b981; box-shadow: inset 0 0 10px #10b981, 0 0 10px #10b981; animation: blink-call 1s infinite alternate; }

.comms-hub-3d:hover .b-green .led-ring.active { animation: none; box-shadow: inset 0 0 20px #10b981, 0 0 20px #10b981; background: rgba(16,185,129,0.2); }


/* HOVER EFFECT: The Receiver lifts off magnetically */
.floating-receiver {
   position: absolute; left: -40px; top: 20px; width: 60px; height: 240px;
   transform-style: preserve-3d;
   transform: translateZ(20px);
   transition: all 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);
}
.comms-hub-3d:hover .floating-receiver {
   transform: translateZ(80px) translateX(-30px) rotateY(-20deg) rotateZ(10deg);
}

.receiver-handle {
   position: absolute; inset: 0; background: linear-gradient(to right, #27272a, #52525b, #27272a);
   border-radius: 30px; border: 1px solid #18181b;
   box-shadow: 10px 10px 20px rgba(0,0,0,0.8), inset 2px 0 5px rgba(255,255,255,0.2);
   display: flex; flex-direction: column; justify-content: space-between; align-items: center;
   z-index: 20; transform-style: preserve-3d;
}
.rx-earpiece, .rx-mouthpiece {
   width: 56px; height: 56px; background: #18181b; border-radius: 50%;
   margin: 4px; border: 2px solid #09090b;
   display: flex; justify-content: center; align-items: center;
   box-shadow: inset 0 0 15px #000; transform: translateZ(5px);
}
.rx-grill {
   width: 40px; height: 40px; border-radius: 50%;
   background-image: radial-gradient(#000 40%, transparent 45%); background-size: 6px 6px;
}
.rx-grip {
   flex-grow: 1; width: 40px; background: #18181b; border-radius: 20px;
   margin: 10px 0; border: 1px solid #27272a; box-shadow: inset 0 0 10px #000;
}

/* Sci-fi "Tether" that appears when picked up */
.energy-tether {
   position: absolute; bottom: 30px; left: 30px; width: 4px; height: 0;
   background: #10b981; filter: blur(2px); opacity: 0;
   transform-origin: bottom center; transform: rotateZ(-40deg);
   transition: all 0.6s; z-index: 10;
}
.comms-hub-3d:hover .energy-tether {
   height: 120px; opacity: 0.6; box-shadow: 0 0 20px 5px #10b981;
}


@keyframes rotate-ring { 100% { transform: rotate(360deg); } }
@keyframes blink-call { 0% { opacity: 0.2; } 100% { opacity: 1; } }
@keyframes wave-anim { 0% { height: 10%; } 100% { height: 100%; } }
</style>