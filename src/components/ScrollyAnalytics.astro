---
// Premium 3D Tablet Mockup (Photorealistic Analytics Device)
---
<div class="tablet-mockup-3d">
   <!-- Titanium Edges -->
   <div class="tablet-edge edge-top"></div>
   <div class="tablet-edge edge-bottom"></div>
   <div class="tablet-edge edge-left"></div>
   <div class="tablet-edge edge-right"></div>
   
   <!-- Back Panel -->
   <div class="tablet-back">
       <div class="camera-module">
          <div class="lens"></div>
          <div class="flash"></div>
       </div>
       <div class="brand-logo">
           <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#71717a" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 2 7 12 12 22 7 12 2"></polygon><polyline points="2 17 12 22 22 17"></polyline><polyline points="2 12 17 22 12"></polyline></svg>
       </div>
   </div>

   <!-- Front Screen -->
   <div class="tablet-front">
      <div class="tablet-screen">
         <div class="screen-glass"></div>
         
         <!-- UI Interface -->
         <div class="tablet-ui">
             <div class="ui-sidebar">
                 <div class="ui-nav"></div><div class="ui-nav active"></div><div class="ui-nav"></div>
             </div>
             <div class="ui-content">
                 <div class="ui-header"><div class="title"></div><div class="avatar"></div></div>
                 <div class="ui-grid">
                     <div class="ui-card wide"></div>
                     <div class="ui-card square"></div>
                     <div class="ui-card square"></div>
                 </div>
             </div>
         </div>

         <!-- Holographic AR Hover Elements (Characteristic Effect) -->
         <div class="ar-interface">
            <div class="ar-panel floating-stats">
                <div class="stat"><div class="lbl">Revenue</div><div class="val">$84.2K</div></div>
                <div class="stat"><div class="lbl">Growth</div><div class="val text-green">+140%</div></div>
            </div>
            
            <div class="ar-chart-bar b1"></div>
            <div class="ar-chart-bar b2"></div>
            <div class="ar-chart-bar b3"></div>
            <div class="ar-chart-bar b4"></div>
            <div class="ar-chart-bar b5"></div>
            
            <div class="ar-ring"></div>
            <div class="ar-glow"></div>
         </div>
      </div>
   </div>
</div>

<style>
/* ========================================
   PREMIUM 3D TABLET MOCKUP (Analytics)
   ======================================== */
.tablet-mockup-3d {
   width: 420px; height: 300px;
   position: relative; transform-style: preserve-3d;
   cursor: pointer;
}

/* Front Face */
.tablet-front {
   position: absolute; inset: 0; transform: translateZ(8px);
   background: #000; border-radius: 20px;
   padding: 16px; /* Bezel */
   box-shadow: inset 0 0 10px rgba(0,0,0,1);
   border: 2px solid #52525b; /* Metal lip */
   transform-style: preserve-3d;
}

/* Screen */
.tablet-screen {
   width: 100%; height: 100%; background: #0f172a; border-radius: 8px;
   position: relative; overflow: hidden; transform-style: preserve-3d;
}

.screen-glass {
   position: absolute; inset: -50%; width: 200%; height: 200%;
   background: linear-gradient(135deg, rgba(255,255,255,0.1) 0%, transparent 40%);
   pointer-events: none; z-index: 50; transform: translateZ(1px);
}

/* Base UI */
.tablet-ui {
   display: flex; width: 100%; height: 100%; padding: 10px; gap: 10px;
   opacity: 0.8; transition: opacity 0.3s ease;
}
.tablet-mockup-3d:hover .tablet-ui { opacity: 0.2; } /* Dim UI to highlight AR */

.ui-sidebar { width: 50px; background: rgba(255,255,255,0.05); border-radius: 6px; display: flex; flex-direction: column; padding: 10px 0; align-items: center; gap: 10px; }
.ui-nav { width: 30px; height: 30px; border-radius: 6px; background: rgba(255,255,255,0.1); }
.ui-nav.active { background: #3b82f6; }

.ui-content { flex: 1; display: flex; flex-direction: column; gap: 10px; }
.ui-header { display: flex; justify-content: space-between; align-items: center; }
.title { width: 100px; height: 16px; background: rgba(255,255,255,0.2); border-radius: 4px; }
.avatar { width: 30px; height: 30px; border-radius: 50%; background: rgba(255,255,255,0.2); }
.ui-grid { display: grid; grid-template-columns: 1fr 1fr; grid-template-rows: 140px 1fr; gap: 10px; flex: 1; }
.ui-card { background: rgba(255,255,255,0.05); border-radius: 8px; border: 1px solid rgba(255,255,255,0.1); }
.wide { grid-column: span 2; }

/* DYNAMIC HOVER: Holographic AR Interface */
.ar-interface {
   position: absolute; inset: 0; transform-style: preserve-3d;
   display: flex; align-items: flex-end; justify-content: center; gap: 15px; padding-bottom: 40px;
   pointer-events: none;
}

.ar-glow {
   position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%) translateZ(1px);
   width: 200px; height: 200px; background: radial-gradient(circle, rgba(0, 245, 255, 0.4) 0%, transparent 60%);
   opacity: 0; transition: opacity 0.5s ease; filter: blur(20px);
}
.tablet-mockup-3d:hover .ar-glow { opacity: 1; }

.ar-chart-bar {
   width: 30px; height: 0; background: linear-gradient(to top, rgba(59,130,246,0.8), rgba(0,245,255,0.9));
   border-radius: 4px 4px 0 0; border: 1px solid rgba(0,245,255,0.5); border-bottom: none;
   box-shadow: 0 0 15px rgba(0,245,255,0.4);
   opacity: 0; transform: translateZ(5px);
   transition: all 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

.tablet-mockup-3d:hover .ar-chart-bar { opacity: 1; }
.tablet-mockup-3d:hover .b1 { height: 60px; transform: translateZ(60px); transition-delay: 0.05s; }
.tablet-mockup-3d:hover .b2 { height: 100px; transform: translateZ(80px); transition-delay: 0.1s; }
.tablet-mockup-3d:hover .b3 { height: 70px; transform: translateZ(100px); transition-delay: 0.15s; }
.tablet-mockup-3d:hover .b4 { height: 140px; transform: translateZ(120px); transition-delay: 0.2s; }
.tablet-mockup-3d:hover .b5 { height: 110px; transform: translateZ(140px); transition-delay: 0.25s; }

.ar-ring {
   position: absolute; bottom: -30px; left: 50%; width: 260px; height: 260px;
   margin-left: -130px; border: 2px dashed rgba(0, 245, 255, 0.6); border-radius: 50%;
   transform: rotateX(80deg) translateZ(5px); opacity: 0;
   transition: all 0.7s cubic-bezier(0.175, 0.885, 0.32, 1);
}
.tablet-mockup-3d:hover .ar-ring {
   opacity: 1; transform: rotateX(75deg) translateZ(30px) scale(1.1);
   animation: scan-spin 10s linear infinite;
}

.floating-stats {
   position: absolute; top: 30px; left: 30px; 
   background: rgba(15, 23, 42, 0.85); backdrop-filter: blur(10px);
   border: 1px solid rgba(0, 245, 255, 0.4); border-radius: 12px;
   padding: 15px; display: flex; gap: 25px; box-shadow: 0 20px 40px rgba(0,0,0,0.5), inset 0 0 10px rgba(0,245,255,0.2);
   opacity: 0; transform: translateZ(5px) scale(0.8);
   transition: all 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275); transition-delay: 0.3s;
}
.tablet-mockup-3d:hover .floating-stats { opacity: 1; transform: translateZ(80px) scale(1); }
.stat { display: flex; flex-direction: column; gap: 5px; }
.lbl { color: #94a3b8; font-size: 0.7rem; font-family: monospace; text-transform: uppercase; }
.val { color: #fff; font-size: 1.4rem; font-weight: bold; font-family: sans-serif; text-shadow: 0 0 10px rgba(255,255,255,0.5); }
.text-green { color: #22c55e; text-shadow: 0 0 10px rgba(34,197,94,0.5); }


/* Back Panel */
.tablet-back {
   position: absolute; inset: 0; transform: translateZ(-8px) rotateY(180deg);
   background: #18181b; border-radius: 20px; border: 2px solid #52525b;
   box-shadow: inset 0 0 50px rgba(0,0,0,0.8);
   display: flex; align-items: center; justify-content: center;
}
.camera-module {
   position: absolute; top: 20px; right: 20px; width: 40px; height: 40px;
   background: rgba(255,255,255,0.05); border-radius: 10px; border: 1px solid rgba(255,255,255,0.1);
   display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 5px;
   box-shadow: 0 5px 10px rgba(0,0,0,0.3); transform: translateZ(2px);
}
.lens { width: 14px; height: 14px; border-radius: 50%; background: #000; border: 2px solid #3f3f46; box-shadow: inset 0 0 5px rgba(0,0,255,0.2); }
.flash { width: 6px; height: 6px; border-radius: 50%; background: #fef08a; opacity: 0.8; }

/* Metallic Edges (16px thick) */
.tablet-edge { position: absolute; background: #3f3f46; box-shadow: inset 0 0 10px rgba(0,0,0,0.9); }
.edge-top { width: 380px; height: 16px; top: -8px; left: 20px; transform: rotateX(90deg); }
.edge-bottom { width: 380px; height: 16px; bottom: -8px; left: 20px; transform: rotateX(-90deg); }
.edge-left { width: 16px; height: 260px; left: -8px; top: 20px; transform: rotateY(-90deg); }
.edge-right { width: 16px; height: 260px; right: -8px; top: 20px; transform: rotateY(90deg); }

@keyframes scan-spin { 100% { transform: rotateX(75deg) translateZ(30px) scale(1.1) rotateZ(360deg); } }
</style>
