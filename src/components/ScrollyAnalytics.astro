<div class="tablet-mockup-3d">
   <!-- Extruded Edges -->
   <div class="tablet-edge edge-top"></div>
   <div class="tablet-edge edge-bottom"></div>
   <div class="tablet-edge edge-left"></div>
   <div class="tablet-edge edge-right"></div>
   
   <!-- Back Panel -->
   <div class="tablet-back">
       <div class="tablet-logo"></div>
   </div>

   <!-- Front Display -->
   <div class="tablet-front">
      <div class="tablet-frame">
         <div class="tablet-camera"></div>
         <div class="tablet-screen" id="analytics-screen-inner">
            <!-- Illustrative Data Dashboard inside -->
            <div class="dashboard-cartoony">
               <!-- Header -->
               <div class="dash-header">
                  <div class="dash-title">ANALYTICS</div>
                  <div class="dash-user"></div>
               </div>

               <!-- Main Chart Area -->
               <div class="chart-box">
                  <div class="bar-chart-3d">
                     <!-- 3D looking columns -->
                     <div class="bar-col c1"><div class="bar-face front"></div><div class="bar-face top"></div></div>
                     <div class="bar-col c2"><div class="bar-face front"></div><div class="bar-face top"></div></div>
                     <div class="bar-col c3"><div class="bar-face front"></div><div class="bar-face top"></div></div>
                     <div class="bar-col c4"><div class="bar-face front"></div><div class="bar-face top"></div></div>
                  </div>
               </div>

               <!-- Mini Stats -->
               <div class="stats-row">
                  <div class="stat-bubble s-green">↑ %84</div>
                  <div class="stat-bubble s-blue">32K</div>
                  <div class="stat-bubble s-purple">NEW</div>
               </div>

               <div class="screen-glare"></div>
            </div>
         </div>
      </div>
   </div>
</div>

<style>
/* ========================================
   CONCRETE 3D TABLET (ANALYTICS)
   ======================================== */
.tablet-mockup-3d {
   width: 450px; height: 320px;
   position: relative;
   transform-style: preserve-3d;
}

/* Base Extrusions (30px thick) */
.tablet-front {
   position: absolute; inset: 0; transform: translateZ(15px);
   background: #111; border-radius: 24px; padding: 15px; border: 2px solid #52525b;
   box-shadow: inset 0 0 10px #000; transform-style: preserve-3d;
}

.tablet-back {
   position: absolute; inset: 0; transform: translateZ(-15px) rotateY(180deg);
   background: #1e293b; border-radius: 24px; border: 2px solid #52525b;
   box-shadow: inset 0 0 60px rgba(0,0,0,0.8);
}
.tablet-logo {
   position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);
   width: 50px; height: 50px; border-radius: 50%; border: 3px solid rgba(255,255,255,0.1);
   background: radial-gradient(circle, rgba(255,255,255,0.1), transparent);
}

.tablet-edge { position: absolute; background: #3f3f46; box-shadow: inset 0 0 10px rgba(0,0,0,0.8); }
.edge-top { width: 410px; height: 30px; top: -15px; left: 20px; transform: rotateX(90deg); }
.edge-bottom { width: 410px; height: 30px; bottom: -15px; left: 20px; transform: rotateX(-90deg); }
.edge-left { width: 30px; height: 280px; left: -15px; top: 20px; transform: rotateY(-90deg); }
.edge-right { width: 30px; height: 280px; right: -15px; top: 20px; transform: rotateY(90deg); }

/* Screen Frame */
.tablet-frame {
   width: 100%; height: 100%; background: #000; border-radius: 12px;
   position: relative; overflow: hidden;
}
.tablet-camera {
   position: absolute; top: 50%; left: -6px; transform: translateY(-50%); width: 8px; height: 8px;
   border-radius: 50%; background: #222; border: 1px solid #111;
   box-shadow: inset 0 0 3px rgba(0,100,255,0.4); z-index: 100;
}

/* Screen Content */
.tablet-screen {
   width: 100%; height: 100%; background: #0f172a; position: relative;
   display: flex; flex-direction: column; overflow: hidden;
}
.dashboard-cartoony {
   width: 100%; height: 100%; padding: 15px; display: flex; flex-direction: column; gap: 15px;
}
.dash-header { display: flex; justify-content: space-between; align-items: center; }
.dash-title { color: #fff; font-size: 1.2rem; font-weight: 800; font-family: monospace; letter-spacing: 2px; }
.dash-user { width: 35px; height: 35px; border-radius: 50%; background: #3b82f6; border: 2px solid #60a5fa; }

/* 3D Looking Charts inside Screen */
.chart-box {
   flex-grow: 1; background: rgba(255,255,255,0.05); border-radius: 10px;
   position: relative; padding: 20px; display: flex; align-items: flex-end; justify-content: space-around;
   border-bottom: 3px solid rgba(255,255,255,0.1);
}
.bar-chart-3d {
   width: 100%; height: 100%; display: flex; align-items: flex-end; justify-content: space-around;
}
.bar-col {
   width: 40px; position: relative; background: transparent; transform-style: preserve-3d;
}
.bar-col .front {
   width: 100%; height: 100%; position: absolute; bottom: 0; left: 0;
   background: linear-gradient(to top, #0ea5e9, #38bdf8); border-radius: 4px 4px 0 0;
   box-shadow: inset 0 -10px 10px rgba(0,0,0,0.3);
}
.bar-col .top {
   width: 100%; height: 15px; position: absolute; top: -15px; left: 0;
   background: #7dd3fc; transform: skewX(-30deg) translateX(7px);
}
.c1 { height: 80%; animation: bar-rise 2s ease-out infinite alternate; }
.c2 { height: 50%; animation: bar-rise 2.5s ease-out infinite alternate; }
.c3 { height: 100%; animation: bar-rise 1.5s ease-out infinite alternate; filter: hue-rotate(50deg); }
.c4 { height: 30%; animation: bar-rise 3s ease-out infinite alternate; filter: hue-rotate(-50deg); }

.stats-row { display: flex; gap: 10px; justify-content: space-between; }
.stat-bubble {
   flex-grow: 1; height: 40px; border-radius: 8px; display: flex; align-items: center; justify-content: center;
   font-weight: 800; font-size: 1rem; color: #fff; text-shadow: 0 2px 2px rgba(0,0,0,0.5);
   border: 2px solid rgba(255,255,255,0.2);
}
.s-green { background: linear-gradient(135deg, #10b981, #059669); }
.s-blue { background: linear-gradient(135deg, #3b82f6, #2563eb); }
.s-purple { background: linear-gradient(135deg, #8b5cf6, #7c3aed); }

.screen-glare {
   position: absolute; top: -50%; left: -50%; width: 200%; height: 200%;
   background: linear-gradient(45deg, transparent 40%, rgba(255,255,255,0.08) 45%, transparent 50%);
   animation: glare 4s infinite linear; pointer-events: none;
}

@keyframes bar-rise { 0% { opacity: 0.8; } 100% { opacity: 1; transform: scaleY(0.95); transform-origin: bottom; } }
@keyframes glare { 0% { transform: translate(-30%, -30%); } 100% { transform: translate(30%, 30%); } }
</style>
